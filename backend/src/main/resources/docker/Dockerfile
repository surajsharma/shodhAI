FROM openjdk:17-slim

WORKDIR /app

# Install timeout (if using timeout inside container instead of outside)
# RUN apt-get update && apt-get install -y coreutils && rm -rf /var/lib/apt/lists/*

# No ENTRYPOINT or CMD - we'll specify commands when running